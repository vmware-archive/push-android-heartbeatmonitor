<?xml version="1.0" encoding="utf-8"?>
<manifest package="io.pivotal.android.push.heartbeatmonitor"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
            android:name="io.pivotal.android.push.receiver.GcmBroadcastReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />

                <category android:name="io.pivotal.android.push.heartbeatmonitor" />
            </intent-filter>
        </receiver>

        <service
            android:name=".PushService"
            android:exported="false" />

    </application>

    <permission
        android:name="io.pivotal.android.push.heartbeatmonitor.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />
    <uses-permission android:name="io.pivotal.android.push.heartbeatmonitor.permission.C2D_MESSAGE" />


</manifest>
